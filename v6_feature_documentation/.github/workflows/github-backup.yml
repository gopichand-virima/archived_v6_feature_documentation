# Auto-generated by enroll-repos.py — do not edit manually
name: GitHub Backup

on:
  push:
    branches: ["**"]
  workflow_dispatch:

jobs:
  backup:
    uses: Virima-Infra/github-backup/.github/workflows/backup-sync.yml@main
    secrets:
      BITBUCKET_USERNAME: ${{ secrets.BITBUCKET_USERNAME }}
      BITBUCKET_TOKEN: ${{ secrets.BITBUCKET_TOKEN }}
      BITBUCKET_SSH_PRIVATE_KEY: ${{ secrets.BITBUCKET_SSH_PRIVATE_KEY }}
