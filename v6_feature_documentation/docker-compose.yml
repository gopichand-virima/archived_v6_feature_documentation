services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: v6-feature-doc-site
    ports:
      - "8080:8080"  # nginx-unprivileged listens on 8080 (non-root cannot bind 80)
    restart: unless-stopped
